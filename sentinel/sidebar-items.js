initSidebarItems({"struct":[["Sentinel","Sentinel is templated on an immutable Request type, a mergeable Claim type. It further takes a Name type to identify claimants. Signature and PublicSignKey type are auxiliary types to handle a user-chosen cryptographic signing scheme."]],"trait":[["Claimable","The Claim type needs to implement this Claimable trait. Sentinel will call verify on the claim with the associated signature and the public signing key that should verify it. Sentinel independently retrieves these public signing keys through the trait get_signing_keys on the original request. When the threshold is reached on a"],["GetSigningKeys","The Request type needs to implement this GetSigningKey trait. Sentinel will call get_signing_keys() the first time it receives a request for which it does not yet have any associated keys."]]});